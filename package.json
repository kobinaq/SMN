{
  "name": "smn",
  "private": true,
  "description": "Social Marketers Network — monorepo. The Next.js app lives in /web.",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix web",
    "lint": "npm run lint --prefix web"
  },
  "engines": {
    "node": ">=20"
  }
}
